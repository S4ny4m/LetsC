#include <stdio.h>
#include <string.h>

int main()
{
    FILE *p;
    char c[200];
    scanf("%[^\n]s", c);
    for (int i = 0; i != '\0'; i++)
    {
        if (c[i] == 'a' || strcmp()||)
    }
    return 0;
}