#include <stdio.h>

int main()
{
    char c[100];
    gets(c);
    int i = stren(c), t, h, e;
    for (i = 0; i < l - 2; i++)
    {
        if (c[i] == 't' || c[0] == 't')
        {
            t++;
        }
        if (c[i] == 'h' || c[0] == 'h')
        {
            h++;
        }
        if (c[i] == 'e' || c[0] == 'e')
        {
            e++;
        }
    }
}